# Environment variables for the Diagram Generator MCP Server

# Optional: Set custom output directory for diagrams
# DIAGRAM_OUTPUT_DIR=/path/to/output/directory

# Optional: Default diagram format (png, jpg, svg, pdf)
# DEFAULT_DIAGRAM_FORMAT=png

# Optional: Default diagram direction (TB, BT, LR, RL)
# DEFAULT_DIAGRAM_DIRECTION=TB

# Optional: Enable debug logging
# DEBUG=true

# Optional: Custom Graphviz path (if not in system PATH)
# GRAPHVIZ_PATH=/usr/local/bin/dot
